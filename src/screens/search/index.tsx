/***
LootSwap - SEARCH SCREEN
***/

import React, {FC} from 'react';
import {
  Container,
  TempText,
} from './styles';

export const SearchScreen: FC<any> = ({route}) => {

  return (
    <Container>
      <TempText> SLKDFJDKLSFJS </TempText>
    </Container>
  );
};

export default SearchScreen;
